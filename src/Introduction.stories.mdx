import { Meta } from '@storybook/addon-docs'

<Meta title="Example/Introduction" />

<style>{``}</style>

# Welcome to SeamLess UI

Where we make UI seamless

Planning

- Understanding the applications that will be consuming and if possible understand future scale needs
- Understanding consuming applications usage of general components (ie: links, buttons, forms, etc..)
- Understanding consuming applications design system

- Decide on how to handle design system constraints
- Decide SCSS / CSS in JS

- Create a migration plan to start isolating general components into the new library

- Expand into loading states

Notes

form state `react-hook-form`

TS Examples

--- Pick
type ModalProps = {
canClose?: boolean
children?: React.ReactNode
onClose: () => void
isTruthy: boolean
variant: 'light' | 'dark'
}
type ComponentProps = Pick<ModalProps, 'isOpen' | 'onClose'>

--- Record
export const gitIcons: Record<GitAccountSource, GitIcons> = {
Bitbucket: 'brand-bitbucket',
GitHub: 'brand-github',
GitLab: 'brand-gitlab',
}
gitIcons[g.source]
